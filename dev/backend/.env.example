# Configurações do servidor
PORT=3000

# URL base para construção de URLs de arquivos (opcional)
# Se não definida, será detectada automaticamente baseada no header Host
# Exemplo para produção: BASE_URL=https://meudominio.com
# Exemplo para desenvolvimento: BASE_URL=http://localhost:3000
BASE_URL=BASE_URL=http://localhost

# Configuração do banco de dados
# DATABASE_URL=sqlite://./data.sqlite

